# ci-sandbox
Sandbox repo to test CI gates: secrets, lint, tests, dependency review, CodeQL.

## Setup
npm run setup

## Before pushing (optional)
npm run verify

ticket-test
commitizen fallback test

commit ai retest
